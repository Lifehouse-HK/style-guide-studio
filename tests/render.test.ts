import {test} from 'node:test';import assert from 'node:assert/strict';import {specimen} from './fixtures.ts';
import {newNode,pair} from '../modules/document.ts';import {render,inline} from '../modules/render.ts';import {publicCatalogue,resolve} from '../modules/references.ts';
test('subsections have inline labels rather than HTML headings and shared tables appear once per output',async()=>{const g=specimen();const sub=newNode('subsection','1');sub.blocks=[{id:'text1',type:'text',text:pair('<script>bad</script>','字句')}];g.nodes[0].children[0].children=[sub];const sch=newNode('schedule','1');sch.heading=pair('Terms','用語');sch.blocks=[{id:'table1',type:'table',caption:pair('Terms','用語'),numbered:true,rows:[['English','繁體中文'],['Dream Team','夢幻團隊']]}];g.nodes.push(sch);const html=await render(g,{layout:'parallel'});assert.equal((html.match(/<table>/g)??[]).length,1);assert.match(html,/@page\{size:A4 landscape\}/);assert.match(html,/&lt;script&gt;/);assert.doesNotMatch(html,/<h[1-6]>\(1\)/);assert.match(html,/<span class="number">\(1\)<\/span>/);});
test('external references select HTML/PDF and use the document-scoped alias consistently',()=>{const g=specimen(),external=specimen();external.id='external-guide';const c=publicCatalogue(external,'https://example.org/guides/','r1');g.aliases[external.id]=pair('2026 Guide','2026年指引');const key=external.id+'#'+external.nodes[0].id;assert.match(resolve(key,g,'en',[c],true).href,/en.pdf#/);assert.match(resolve(key,g,'en',[c]).label,/2026 Guide/);assert.doesNotMatch(inline('**<b>**',g,'en'),/<b>/);});
