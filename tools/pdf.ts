import {readFile,mkdir,writeFile} from 'node:fs/promises';import {resolve,join} from 'node:path';import puppeteer from 'puppeteer';
import {parseDocument} from '../modules/amendments.ts';import {render,type Layout} from '../modules/render.ts';
const [input,output,sourceFile]=process.argv.slice(2);if(!input||!output)throw Error('Usage: npm run pdf -- document.json output-directory [source-guide.json]');
const doc=parseDocument(await readFile(input,'utf8')),source=sourceFile?parseDocument(await readFile(sourceFile,'utf8')):undefined;if(source&&source.type!=='guide')throw Error('Source must be a principal Guide.');
await mkdir(output,{recursive:true});const browser=await puppeteer.launch();try{for(const layout of (doc.mode==='parallel'?['en','zh','parallel']:[doc.mode]) as Layout[]){const html=await render(doc,{layout,pdf:true},source);await writeFile(join(output,layout+'.html'),html);const page=await browser.newPage();await page.setContent(html,{waitUntil:'load'});await page.evaluate(()=>document.fonts.ready);await page.pdf({path:resolve(output,layout+'.pdf'),preferCSSPageSize:true,printBackground:true,tagged:true,outline:true});await page.close();}}finally{await browser.close();}
